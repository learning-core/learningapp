name: learningapp
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"


dependencies:
  cupertino_icons: ^1.0.2
  micro_core:
    git:
      url: https://github.com/learning-core/micro_core.git
      ref: main
  learningapp_login:
    git:
      url: https://github.com/learning-core/learningapp_login.git
      ref: main
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

